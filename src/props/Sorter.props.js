const props = {
  value: {},
  label: {},
  disabled: {
    default: false,
    type: Boolean
  }
}

const container = {
  ...props
}

const renderable = {
  ...props
}

export default {
  container,
  renderable
}
